@model WebUI.ViewModels.AlunoViewModel

@{
    ViewBag.Title = "Lista";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section Styles {

}

@section Scripts {

    <script type="text/javascript">
        $(".confirmDelete").confirm({

            text: "Deseja realmente excluir este item?",
            confirm: function (button) {

                $('#LoadingModal').modal({ backdrop: 'static', keyboard: false });
                //$("#cod").val($(button).attr("item-id"))
                //$("#DeleteForm").submit();

                var data = { cod: $(button).attr("data-id") };
                var action = $(button).attr("data-action");
                //alert(JSON.stringify(data) + "#" + JSON.stringify(action));
                var fsuccess = function (response) {

                    location.reload(true);
                }

                postData(action, data, fsuccess);
            },
            cancel: function (button) {

                //Do nothing
            },
            confirmButton: "Sim",
            cancelButton: "N&#227o"
        });
    </script>

}

<!-- Page Heading -->
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">
            Alunos <i class="fas fa-angle-right"></i> Lista
        </h1>
    </div>
</div>
<!-- /.row -->
<!-- Start feedback message code -->

@RenderPage("~/Views/Shared/_Messages.cshtml")

<!-- End feedback message code -->
@*<form name="DeleteForm" action="/Usuarios/Excluir/" id="DeleteForm" method="post">
        @Html.AntiForgeryToken()
        <input type="hidden" id="cod" name="cod" />
    </form>*@

<form name="ListaForm" id="ListaForm" action="/Alunos/Lista" method="post" role="form">
    @Html.AntiForgeryToken()
    <div class="row">

        <div class="col-md-12">

            <div class="form-group">
                <div class="text-left d-inline-block">
                    <button type="reset" class="btn btn-default" onclick="location.href='/Alunos/Novo'">
                        <i class="fas fa-plus"></i>
                        <span>Novo Aluno</span>
                    </button>
                </div>
                <div class="float-right d-inline-block">
                    <input type="text" name="nome" class="form-control input-search" />
                    <button type="reset" class="btn btn-default btn-search" onclick="document.getElementById('ListaForm').submit()">
                        <i class="fas fa-search"></i>
                        Buscar
                    </button>
                </div>
            </div>

        </div>

    </div>

</form>

<div class="row">
    @if (Model.Alunos != null)
    {
        <div class="col-md-12 table-responsive">
            <table class="table table-bordered table-hover">
                <thead>
                    <tr>
                        <th class=""></th>
                        <th class=""></th>
                        <th class="col-md-6">Nome</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Alunos.Count() == 0)
                    {
                        <tr>
                            <td colspan="4"><label>Nenhum registro encontrado</label></td>
                        </tr>
                    }
                    else
                    {
                        foreach (var item in Model.Alunos)
                        {
                            <tr>
                                <td style="text-align: center;"><i class="glyphicon glyphicon-remove confirmDelete" data-id="@item.ID" data-action="/Alunos/Excluir/" style="cursor:pointer;"></i></td>
                                <td style="text-align: center;"><span onclick="location.href='/Alunos/Alterar/@item.ID'" class="glyphicon glyphicon-pencil" style="cursor:pointer;" /></td>
                                <td>@item.Nome</td>
                            </tr>
                        }
                    }

                </tbody>
            </table>
        </div>
    }
</div>
